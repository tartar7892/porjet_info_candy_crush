#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#include "head.h"

char grille[TAILLE_LIGNE][TAILLE_COLONNE];
int niveau ;

int main() {
    srand(time(NULL));

menu();
    return 0;
}
